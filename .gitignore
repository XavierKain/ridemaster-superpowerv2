# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
server.log

# Temporary files
*.tmp
*.temp
*.bak

# Node modules (if you add Node.js later)
node_modules/
dist/
build/

# Environment variables
.env
.env.local
.env.*.local
plugins/ridemaster/vendor/stripe/stripe-php/README.md

# Plugin build artifacts (versioned zips are release outputs, not sources)
plugins/*.zip

# Debug screenshots dropped at the repo root during Playwright sessions
/*.png
.playwright-mcp/

# _archive/ is tracked (see _archive/README.md) except these two heavy folders,
# which stay on disk but out of git: 32 MB of comparison screenshots and
# reinstallable npm dependencies.
_archive/elementor-automation/screenshots/
_archive/elementor-automation/node_modules/
